models:
  embedding: "mxbai-embed-large"
  llm:
    type: "local"
    model: "llama3.2:1b"

chunking:
  size: 500
  overlap: 100
  strategy: "fixed"

retrieval:
  top_k: 10

citations:
  unique_id_format: "{author}{title_words}{year}"
